# Test + lint tooling for the SignalOps backend (lean: no Vertex/GCP libs, which
# the app imports lazily and the test suite does not exercise).
# Install into a venv:  pip install -r requirements-dev.txt

# --- runtime subset needed to import and test the app ---
fastapi>=0.115.0
opensearch-py>=2.7.1
redis>=5.0.0
python-dotenv>=1.0.1
pydantic>=2.9.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
apscheduler>=3.10.0

# --- test + lint ---
pytest>=8.3.0
pytest-cov>=5.0.0
httpx>=0.27.0                 # FastAPI TestClient transport
testcontainers[opensearch,postgres,redis]>=4.8.0
ruff>=0.7.0
